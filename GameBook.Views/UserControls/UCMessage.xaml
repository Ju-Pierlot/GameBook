<UserControl x:Class="GameBook.Views.UserControls.UCMessage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Source={StaticResource Locator}, Path=MessageVM}">
    <StackPanel Orientation="Horizontal" Background="{Binding Color}" Height="30" VerticalAlignment="Bottom">
        <TextBlock VerticalAlignment="Center" Text="{Binding Message}" Margin="30 0 30 0" FontFamily="Cambria" FontSize="12 pt" FontStyle="Normal" FontWeight="Normal" TextWrapping="Wrap" Foreground="#866"/>
    </StackPanel>
</UserControl>
