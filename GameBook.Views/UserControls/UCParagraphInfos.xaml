<UserControl x:Class="GameBook.Views.UserControls.UCParagraphInfos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Source={StaticResource Locator}, Path=ParagraphInfosVM}">
    <StackPanel CanHorizontallyScroll="False" CanVerticallyScroll="True">
        <TextBlock Height="Auto" Margin="30 30 30 0"  Text="{Binding Path=BookTitle}" FontFamily="Cambria" FontSize="16 pt" FontStyle="Normal" FontWeight="Bold" TextWrapping="Wrap"/>
        <TextBlock Margin="30 5 30 0" Text="{Binding Path=Number}" FontFamily="Cambria" FontSize="10 pt" FontStyle="Normal" FontWeight="Bold"  TextWrapping="Wrap" Foreground="#666"/>
        <TextBlock Height="Auto" Margin="30 30 30 0" Text="{Binding Label}" FontFamily="Cambria" FontSize="14 pt" FontStyle="Normal" FontWeight="Normal"  TextWrapping="Wrap"/>
    </StackPanel>
</UserControl>
